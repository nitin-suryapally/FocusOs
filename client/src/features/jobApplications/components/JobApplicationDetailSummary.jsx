import { formatJobApplicationStatus } from "../jobApplicationOptions";

const formatDate = (date) => date ? new Intl.DateTimeFormat(undefined, { dateStyle: "medium" }).format(new Date(date)) : "Not scheduled";
const formatTimestamp = (date) => new Intl.DateTimeFormat(undefined, { dateStyle: "medium", timeStyle: "short" }).format(new Date(date));

export const JobApplicationDetailSummary = ({ application }) => <section className="rounded-[28px] border border-outline-variant/70 bg-surface/82 p-6 shadow-card backdrop-blur-sm sm:p-8"><p className="text-label-sm uppercase tracking-[0.2em] text-primary">{formatJobApplicationStatus(application.status)}</p><h1 className="mt-3 text-3xl font-semibold tracking-[-0.03em] text-on-surface sm:text-4xl">{application.company}</h1><p className="mt-3 text-body-lg text-on-surface-variant">{application.role}</p><dl className="mt-8 grid gap-5 border-t border-outline-variant/60 pt-5 sm:grid-cols-2"><div><dt className="text-label-sm uppercase tracking-[0.15em] text-on-surface-variant">Follow up</dt><dd className="mt-2 text-body-md text-on-surface">{formatDate(application.followUpDate)}</dd></div><div><dt className="text-label-sm uppercase tracking-[0.15em] text-on-surface-variant">Application</dt><dd className="mt-2 text-body-md text-on-surface">{application.applicationUrl ? <a href={application.applicationUrl} target="_blank" rel="noreferrer" className="text-primary underline">Open application</a> : "Not saved"}</dd></div></dl>{application.notes ? <div className="mt-8 border-t border-outline-variant/60 pt-5"><p className="text-label-sm uppercase tracking-[0.15em] text-on-surface-variant">Notes</p><p className="mt-3 whitespace-pre-wrap text-body-md text-on-surface">{application.notes}</p></div> : null}<div className="mt-8 border-t border-outline-variant/60 pt-5"><p className="text-label-sm uppercase tracking-[0.15em] text-on-surface-variant">History</p><dl className="mt-3 grid gap-5 sm:grid-cols-2"><div><dt className="text-body-sm text-on-surface-variant">Created</dt><dd className="mt-1 text-body-md text-on-surface">{formatTimestamp(application.createdAt)}</dd></div><div><dt className="text-body-sm text-on-surface-variant">Last updated</dt><dd className="mt-1 text-body-md text-on-surface">{formatTimestamp(application.updatedAt)}</dd></div></dl></div></section>;