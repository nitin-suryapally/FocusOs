const API_BASE_URL = import.meta.env.VITE_API_BASE_URL?.replace(/\/$/, "") || "";
const parse = async (response, fallback) => { const data = await response.json().catch(() => ({})); if (!response.ok) throw new Error(data.message || fallback); return data; };
export const fetchJobApplicationsRequest = async (token) => parse(await fetch(`${API_BASE_URL}/api/job-applications`, { headers: { Authorization: `Bearer ${token}` } }), "Unable to load job applications.");
export const fetchJobApplicationRequest = async (token, id) => parse(await fetch(`${API_BASE_URL}/api/job-applications/${id}`, { headers: { Authorization: `Bearer ${token}` } }), "Unable to load job application.");
export const createJobApplicationRequest = async (token, payload) => parse(await fetch(`${API_BASE_URL}/api/job-applications`, { method: "POST", headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` }, body: JSON.stringify(payload) }), "Unable to create job application.");export const updateJobApplicationRequest = async (token, id, payload) => parse(await fetch(`${API_BASE_URL}/api/job-applications/${id}`, { method: "PATCH", headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` }, body: JSON.stringify(payload) }), "Unable to update job application.");
export const deleteJobApplicationRequest = async (token, id) => parse(await fetch(`${API_BASE_URL}/api/job-applications/${id}`, { method: "DELETE", headers: { Authorization: `Bearer ${token}` } }), "Unable to delete job application.");
